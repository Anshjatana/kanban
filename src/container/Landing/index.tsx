const Landing = () => {
  return (
    <div>
      // This will contain: // 1. Welcome section with name input // 2.
      Preview/demo section of kanban features // 3. Theme toggle // 4. CTA to
      navigate to /kanban
    </div>
  );
};

export default Landing;
